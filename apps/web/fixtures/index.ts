/**
 * DEVIN-OWNED (task 3). Scaffold: replace `null` with a full `Fixtures` object.
 * Until then, DEMO_MODE ingestion uses the stopgap samples in lib/ingest/sample-emails.ts.
 */
import type { Fixtures } from '@weave/shared/contracts';

export const fixtures: Fixtures | null = null;
